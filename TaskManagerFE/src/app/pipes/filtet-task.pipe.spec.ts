import { FiltetTaskPipe } from './filtet-task.pipe';

describe('FiltetTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltetTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
